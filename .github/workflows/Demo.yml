name: Neofetch and Disk Space
on: push
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps:
      - name: Install neofetch
        run:
              sudo apt install neofetch
      - name: Run neofetch
        run: neofetch

      - name: Check df -h
        run: df -h
    
      
